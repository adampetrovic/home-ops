{
  "id": "a754f015",
  "title": "Close re-created Renovate PR for unpoller v3.0.0",
  "tags": [
    "home-ops",
    "renovate",
    "unpoller"
  ],
  "status": "open",
  "created_at": "2026-05-09T01:40:31.987Z"
}

When Renovate re-opens/re-creates the `ghcr.io/unpoller/unpoller` v3.0.0 PR in `home-ops`, close it with a comment referencing upstream issue https://github.com/unpoller/unpoller/issues/1002.

Context:
- PR #2600 (`v2.39.0 → v3.0.0`) was merged, then immediately reverted on `main` because `unpoller` panicked on startup with Prometheus descriptor conflicts:
  `descriptors reported by collector have inconsistent label names or help strings ... unpoller_device_uptime_seconds`
- Upstream maintainer comment recommends downgrade.
- Comment should note that the v3.0.0 release caused a startup crash and did not include a breaking-change alert/release-note warning for this Prometheus breakage.

Suggested comment:

> Closing this update for now. v3.0.0 crashes at startup in our Prometheus configuration with `descriptors reported by collector have inconsistent label names ... unpoller_device_uptime_seconds`; see upstream issue https://github.com/unpoller/unpoller/issues/1002. We already tried merging this and had to roll it back. The v3.0.0 release did not include a breaking-change alert/release-note warning for this regression, so we’ll wait for a fixed release before trying again.
