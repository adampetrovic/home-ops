# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: fernwood-booker-nicole
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.6.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      main:
        annotations:
          reloader.stakater.com/auto: 'true'
        containers:
          main:
            image:
              repository: ghcr.io/adampetrovic/fernwood-booker
              tag: 1.0.0@sha256:e791f603e73522e592994a5f6f20ca0432e65b1e14ca38b63c4764bfbab81144
            envFrom:
              - secretRef:
                  name: fernwood-booker-secret-nicole
            env:
              TZ: ${TIMEZONE}
              AUTOBOOK_CONFIG: /config/config-nicole.yaml
            resources:
              requests:
                cpu: 100m
                memory: 100Mi
              limits:
                memory: 250Mi
    service:
      main:
        enabled: false
    persistence:
      config-file:
        enabled: true
        type: configMap
        name: fernwood-configmap-nicole
        readOnly: true
        globalMounts:
          - path: /config/config-nicole.yaml
            subPath: config-nicole.yaml
