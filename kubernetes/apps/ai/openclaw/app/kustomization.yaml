# ABOUTME: Kustomization for OpenClaw app resources
# ABOUTME: Includes external secrets and helm release manifests
---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  - ./helmrelease.yaml
