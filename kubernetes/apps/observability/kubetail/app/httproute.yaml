---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kubetail
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
  hostnames:
    - kubetail.petrovic.network
  rules:
    - backendRefs:
        - name: kubetail-dashboard
          port: 8080
